// Criar uma lista vazia chamada listaGenerica
let listaGenerica = [];

// Criar uma lista de linguagens de programação
let linguagensDeProgramacao = ['JavaScript', 'C', 'C++', 'Kotlin', 'Python'];

// Adicionar elementos à lista linguagensDeProgramacao
linguagensDeProgramacao.push('Java', 'Ruby', 'GoLang');

// Criar uma lista com 3 nomes
let nomes = ['Alice', 'Bob', 'Charlie'];

// Exibir no console apenas o primeiro elemento da lista de nomes
console.log('Primeiro elemento da lista de nomes:', nomes[0]);

// Exibir no console apenas o segundo elemento da lista de nomes
console.log('Segundo elemento da lista de nomes:', nomes[1]);

// Exibir no console apenas o último elemento da lista de nomes
console.log('Último elemento da lista de nomes:', nomes[nomes.length - 1]);
